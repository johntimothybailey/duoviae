import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  /**
   * TODO Move this to an "Atomic" style
   */
  IntroText: {
    textAlign: 'center',
    fontWeight: 'normal',
    paddingBottom: 16
  }
})
